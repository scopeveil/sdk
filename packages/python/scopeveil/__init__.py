"""ScopeVeil Python SDK — placeholder.

The reference implementation lives in ``packages/typescript`` of this
repository. The Python wrapper will mirror that design (Proxy-style wrap
around the OpenAI / Anthropic clients, metadata-only transport).
"""

__version__ = "0.0.0"


class ScopeVeil:
    """Placeholder. Coming soon — see README and the TypeScript SDK."""

    def __init__(self, *_, **__) -> None:
        raise NotImplementedError(
            "The Python SDK is not yet implemented. Use @scopeveil/sdk on npm "
            "for now. Track progress at https://github.com/scopeveil/sdk."
        )
